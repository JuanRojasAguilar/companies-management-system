'use client'

import test from "@/components/root/text"
import React from "react"

const styles = {
    mainDiv: "flex justify-center items-center w-screen min-h-screen",
}

const RegisterPage = () => {
    return (
        <main className={styles.mainDiv}>
            <test/>
        </main>
    );
};

export default RegisterPage;

